{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://zabbix.com/7.0/api/usermacro/usermacro.updateglobal",
  "title": "usermacro.updateglobal",
  "description": "Update existing global macros. The globalmacroid property must be defined for each global macro; all other properties are optional and only the passed properties will be updated.",
  "$comment": "Source: https://www.zabbix.com/documentation/7.0/en/manual/api/reference/usermacro/updateglobal",
  "$defs": {
    "globalMacro": {
      "type": "object",
      "properties": {
        "globalmacroid": {
          "type": "string",
          "description": "ID of the global macro. Property behavior: read-only; required for update operations."
        },
        "macro": {
          "type": "string",
          "description": "Macro string. Property behavior: required for create operations."
        },
        "value": {
          "type": "string",
          "description": "Value of the macro. Property behavior: write-only if type is set to \"Secret macro\"; required for create operations."
        },
        "type": {
          "type": "integer",
          "enum": [0, 1, 2],
          "description": "Type of macro. Possible values: 0 - (default) Text macro; 1 - Secret macro; 2 - Vault secret."
        },
        "description": {
          "type": "string",
          "description": "Description of the macro."
        }
      },
      "required": ["globalmacroid"],
      "additionalProperties": false
    }
  },
  "oneOf": [
    {"$ref": "#/$defs/globalMacro"},
    {"type": "array", "items": {"$ref": "#/$defs/globalMacro"}, "minItems": 1}
  ]
}
