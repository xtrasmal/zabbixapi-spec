{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://zabbix.com/6.0/api/settings/settings.get",
  "title": "settings.get",
  "description": "Retrieve the common administration settings object.",
  "$comment": "Source: https://www.zabbix.com/documentation/6.0/en/manual/api/reference/settings/get",
  "type": "object",
  "properties": {
    "output": {
      "oneOf": [
        { "type": "array", "items": { "type": "string" } },
        { "enum": ["extend", "count"] }
      ],
      "description": "This is the only parameter supported by settings.get. Object properties to be returned. Default: extend."
    }
  },
  "additionalProperties": false
}
