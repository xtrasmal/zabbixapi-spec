{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://zabbix.com/6.0/api/hostgroup/hostgroup.massupdate",
  "title": "hostgroup.massupdate",
  "description": "This method allows to replace hosts and templates with the specified ones in multiple host groups.",
  "$comment": "Source: https://www.zabbix.com/documentation/6.0/en/manual/api/reference/hostgroup/massupdate",
  "type": "object",
  "properties": {
    "groups": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "groupid": {
            "type": "string",
            "description": "ID of the host group."
          }
        },
        "required": ["groupid"],
        "additionalProperties": false
      },
      "description": "Host groups to be updated. The host groups must have the groupid property defined."
    },
    "hosts": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "hostid": {
            "type": "string",
            "description": "ID of the host."
          }
        },
        "required": ["hostid"],
        "additionalProperties": false
      },
      "description": "Hosts to replace the current hosts on the given host groups. All other hosts, except the ones mentioned, will be excluded from host groups. Discovered hosts will not be affected. The hosts must have the hostid property defined."
    },
    "templates": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "templateid": {
            "type": "string",
            "description": "ID of the template."
          }
        },
        "required": ["templateid"],
        "additionalProperties": false
      },
      "description": "Templates to replace the current templates on the given host groups. All other templates, except the ones mentioned, will be excluded from host groups. The templates must have the templateid property defined."
    }
  },
  "required": ["groups", "hosts", "templates"],
  "additionalProperties": false
}
